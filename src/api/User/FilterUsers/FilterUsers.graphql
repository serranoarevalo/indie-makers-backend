enum UserState {
  UPDATED
  FIRE
  SHIPPED
}

type FilterUsersResponse {
  ok: Boolean!
  error: String
  makers: [User]
}

type Query {
  FilterUsers(status: UserState!, page: Int, take: Int): FilterUsersResponse!
}
