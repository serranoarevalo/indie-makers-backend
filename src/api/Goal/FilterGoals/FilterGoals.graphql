enum GoalStatus {
  PENDING
  COMPLETED
}

type FilterGoalsResponse {
  ok: Boolean!
  error: String
  users: [User]
}

type Query {
  FilterGoals(status: GoalStatus!, page: Int, take: Int): FilterGoalsResponse!
}
