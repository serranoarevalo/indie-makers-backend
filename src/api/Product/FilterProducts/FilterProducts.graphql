enum ProductState {
  NEW
  UPDATED
  LAUNCHED
  HELP
  FEATURED
}

type FilterProductsResponse {
  ok: Boolean!
  error: String
  products: [Product]
}

type Query {
  FilterProducts(status: ProductState!, page: Int): FilterProductsResponse!
}
